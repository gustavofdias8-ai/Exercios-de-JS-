const nomes=("Gustavo, joão, Enzo, matheus, mingau")
nomes.forEach((nomes, index) => {
    console.log(nomes)
}

for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}